# application-dev.yaml
spring:
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

  config:
    import: optional:file:.env[.properties]

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
